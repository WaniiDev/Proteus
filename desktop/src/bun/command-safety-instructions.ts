export const COMMAND_SAFETY_INSTRUCTIONS = `Commands and process termination require user approval. Commands run directly on the Windows host from the selected workspace because Mastra LocalSandbox has no OS isolation on Windows. Keep commands scoped to that workspace, prefer bounded foreground commands, use background processes only when genuinely necessary, and never claim a command ran before its native tool result succeeds.`;
